{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": [
    "Typecheck the Sandpack demo fixtures (plan 195 item 5c).",
    "",
    "`demoUsers.ts` already declares `DemoUserContext { plan: UserPlanContext }`",
    "and closes with `satisfies Record<DemoUserId, DemoUser>` — the types were",
    "right and applied. What was missing is that NOTHING RAN THEM: pages-build's",
    "check chain is lint + astro build + link-check, and `astro build` bundles",
    "standalone .ts without type-checking it.",
    "",
    "So all six demo users kept `plan: { id }` months after the field was renamed",
    "to `handle`, and the next docs deploy would have installed SDK 0.4.0 into",
    "every Sandpack sandbox and denied every entitlement check in every live demo.",
    "",
    "Scoped to `src/sandpack/**` on purpose: a whole-package typecheck needs",
    "`astro sync` for the `astro:content` virtual modules and turns up unrelated",
    "pre-existing errors in the components. That cleanup is worth doing and is",
    "not this. These fixtures are the ones that feed the live playground."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": []
  },
  "include": ["src/sandpack/**/*.ts"]
}
