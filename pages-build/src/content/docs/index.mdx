---
title: Getting Started
description: Install the CLI and SDK, register an account, instrument a React app with slots and gates, and manage your monetization config — from zero to live.
---

import { Steps, Tabs, TabItem, Card, CardGrid, Aside } from '@astrojs/starlight/components';

RevTurbine is web SaaS monetization in a single SDK: placement decisioning, entitlements, and conversion experiments — without a code deploy for every change. This guide takes you from install to a live placement and entitlement gate, then shows how to manage your monetization config.

It's **additive** — if RevTurbine is disconnected, misconfigured, or every placement is paused, your product keeps working and slots simply render nothing.

## 1. Install the CLI and SDK

Two packages, both on the public npm registry — no auth token or registry config.

- **`@revturbine/cli`** — manage your monetization config (plans, entitlements, placements) and change sets from the terminal.
- **`@revturbine/sdk`** — render placements and check entitlements in your app.

Install the CLI globally:

```bash
npm install -g @revturbine/cli
revturbine --help
```

Add the SDK to your React app:

<Tabs>
  <TabItem label="npm">
  ```bash
  npm install @revturbine/sdk
  ```
  </TabItem>
  <TabItem label="pnpm">
  ```bash
  pnpm add @revturbine/sdk
  ```
  </TabItem>
  <TabItem label="yarn">
  ```bash
  yarn add @revturbine/sdk
  ```
  </TabItem>
</Tabs>

## 2. Register a RevTurbine account

Create an account in the studio, then connect the CLI to it.

<Steps>

1. **Create your account.** Sign up at the studio — [**revturbine.com/app**](https://revturbine.com/app) — or headlessly from the terminal:

    ```bash
    revturbine signup
    ```

2. **Authorize the CLI on this machine.** This opens a browser device-flow login against your account:

    ```bash
    revturbine login
    ```

    Your token is stored at `~/.revturbine/credentials.json` (mode `0600`) and scoped to your tenant.

3. **Confirm you're connected.** `status` shows your active config and recent releases:

    ```bash
    revturbine status
    ```

</Steps>

<Aside type="tip">
You don't need an account to try the SDK locally — `local_only` runtime mode (used in the next step) runs entirely client-side from a bundled config. The account is for managing your live config and connecting the SDK in server mode.
</Aside>

## 3. Instrument your app

Wrap your app once, then add **slots** (where placements render) and **gates** (entitlement checks).

:::note[Not using React?]
The walkthrough below is React because that is the shortest path to something on
screen. **RevTurbine does not require React.** The decision engine is
framework-agnostic and ships as its own entry point:

```ts
import { initRevTurbine } from '@revturbine/sdk/headless';
```

That entry pulls in no React at all, so it runs on Svelte, Vue, plain
TypeScript, and Node servers. You get the same entitlement checks and placement
decisions; what you give up is the prebuilt `<Slot>` / `<Gate>` components, and
you render the resolved content yourself.

See the [Headless API guide](/guides/headless-api/) for the session, controller
and gate surface, and [Server-side](/getting-started/server-side/) for Node. The
Python and Rust SDKs evaluate the same Playbook with byte-identical results.
:::

<Steps>

1. **Wrap your app in `RevTurbineProvider`.** This example runs in `local_only` mode — no backend, no network calls — from an `playbook.json` you ship with your app (see step 4 for where it comes from).

    ```tsx title="src/App.tsx"
    import { RevTurbineProvider } from '@revturbine/sdk';
    import playbook from './playbook.json';
    import { useMemo } from 'react';

    function App() {
      const options = useMemo(() => ({
        localRuntime: { playbook },
        user: { id: 'user_123', plan_handle: 'starter' },
        // UI path resolvers connect placement CTAs to your app's navigation.
        uiPathResolvers: {
          navigate_to_plans: () => { window.location.href = '/pricing'; },
          open_checkout_modal: (ctx) => { console.log('Checkout:', ctx.plan_handle); },
        },
      }), []);

      return (
        <RevTurbineProvider options={options}>
          <YourApp />
        </RevTurbineProvider>
      );
    }
    ```

    <Aside type="note">
    To run against your live RevTurbine account instead, swap `localRuntime` for server options (`tenantId` + `endpoint`). See [Runtime modes](/guides/runtime-modes/).
    </Aside>

2. **Add a slot.** Drop a surface slot where you want a placement to appear. It renders the matching placement for the current user — or nothing.

    ```tsx title="src/components/Dashboard.tsx"
    import { Slot } from '@revturbine/sdk';

    function Dashboard() {
      return (
        <div>
          <h1>Dashboard</h1>
          {/* RevTurbine renders an upgrade banner here — or nothing */}
          <Slot id="dashboard_top_banner" surfaceTemplateIds={["banner_placement"]} />
        </div>
      );
    }
    ```

    `<Slot>` is the one surface-slot component — it covers banners, modals, in-page content, buttons, and event-triggered messages alike. To drive rendering yourself instead, use the [`usePlacement`](/guides/headless-api/) hook.

3. **Gate a feature.** Wrap gated UI in `<Gate>`. It renders its children when the user is entitled, and automatically shows the configured upgrade placement on denial — no manual check.

    ```tsx title="src/components/BatchExport.tsx"
    import { Gate } from '@revturbine/sdk';

    function BatchExport() {
      return (
        <Gate id="batch_export_gate" can="batch_export">
          <BatchExportButton />
        </Gate>
      );
    }
    ```

    `can="batch_export"` is shorthand for `check={{ entitlement: 'batch_export' }}`. For custom deny UI, use the `useCan` hook — it returns `{ can, limited, result }`:

    ```tsx
    import { useCan } from '@revturbine/sdk';

    function BatchExport() {
      const { can, limited, result } = useCan('batch_export');
      if (!can) return <UpgradePrompt reason={result?.reason} />;
      return <BatchExportButton warnLowBalance={limited} />;
    }
    ```

    `can` is `true` while the user is entitled — including the `limited` (running-low) state, so gate on `!can`, not `!allowed`. For an imperative check (server-side or in an event handler), call `await rt.can('batch_export')`. See the [Entitlements guide](/guides/entitlements/).

</Steps>

## 4. Manage your Exported Config

Your monetization model — **plans, entitlements, entitlement rules, segments, surface templates, and placements** — is a single portable snapshot called an **Playbook**. It's what the SDK reads (bundled in local mode, or served live), and you can author it two ways.

<CardGrid>
  <Card title="RevTurbine UI (studio)" icon="laptop">
    Edit visually at [**revturbine.com/app**](https://revturbine.com/app) — define plans, entitlements, targeting rules, and placements, preview their impact, and deploy. Export an `playbook.json` snapshot anytime for local mode.
  </Card>

  <Card title="RevTurbine CLI + Agent" icon="setting">
    Treat config as code. Author or edit it by hand — or with the RevTurbine **AI agent** (in the studio, or via an MCP-connected coding agent) — then validate and ship it from the terminal.
  </Card>
</CardGrid>

The CLI ships config through reviewable **change sets** (`draft → submit → approve → deploy`):

```bash
revturbine validate ./playbook.json # schema-validate locally, no writes
revturbine diff                            # compare your file against the live config
revturbine upload ./playbook.json   # stage a draft change set
revturbine launch --draft                  # submit → approve → launch the draft
```

<Aside type="tip">
`revturbine download --live --save playbook.json` pulls your live config back down — handy for bundling into the SDK's `local_only` mode.
</Aside>

---

## Why RevTurbine

<CardGrid>
  <Card title="Decisioning, not just rendering">
    RevTurbine decides — caps, cooldowns, eligibility, and priority across competing placements. One `<Slot>` (or `usePlacement()`) returns the winner. No client-side orchestration, no homegrown segment logic. ML-scored propensity is on the roadmap, not shipped — see [Beyond gating](/guides/beyond-gating/).
  </Card>

  <Card title="Entitlements and placements in one engine, linked to billing">
    A `<Slot>` surfaces usage warnings, trial nudges, and lifecycle prompts tied to billing state; `rt.can()` confirms or denies feature access. Pair them to gate a feature *and* surface the customer-specific upgrade prompt that converts it. Like RevenueCat for SaaS.
  </Card>

  <Card title="PM- and marketer-controlled, dev-decoupled">
    Drop a slot once. Your PM, growth, marketing, or CRO team changes content, targeting, and CTAs in the studio — no engineering tickets, no deploys. UI path resolvers keep the navigation contract in your code; everything else iterates without you.
  </Card>

  <Card title="Framework-agnostic, and the same answer everywhere">
    The React components are a convenience, not the product. `@revturbine/sdk/headless` imports no React and runs on Svelte, Vue, plain TypeScript, or a Node server; Python and Rust SDKs evaluate the same Playbook. Every one of them runs the same engine over the same inputs, and a cross-language gate diffs their decisions byte-for-byte — so a check answers identically wherever you make it. See [Headless API](/guides/headless-api/).
  </Card>
</CardGrid>

## What the SDK can do

| Capability | API | Description |
|---|---|---|
| **Placements** | `Slot` / `usePlacement()` | Render upgrade banners, modals, toasts, and buttons based on targeting rules |
| **Entitlements** | `Gate` / `rt.can()` | Gate features, enforce usage limits, check credits and seats |
| **Usage tracking** | `rt.update()` | Track consumption for quota meters and passive placement triggers |
| **Events** | `rt.track()` | Behavioral signals for targeting and analytics |
| **Interactions** | `dismiss()` / `snooze()` / `convert()` | CTA lifecycle tracking |
| **Trial status** | `rt.getTrialStatus()` | Current trial state and countdown |

## Next steps

- [Quickstart](/getting-started/quickstart/) — the detailed local-mode walkthrough, including every UI path action type
- [Entitlement gates](/guides/entitlements/) — gate features and enforce usage limits
- [Runtime modes](/guides/runtime-modes/) — local-only, server, and custom endpoints
- [Interactive playground](/playground/) — live, editable SDK scenarios in your browser
- [Component gallery](/components/) — interactive demos of every built-in slot component
- [API reference](/api/) — full TypeDoc reference for all exports
