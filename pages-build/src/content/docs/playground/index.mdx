---
title: Interactive Playground
description: Every live, editable SDK example in one place — each runs in local_only mode with a demo revturbine.playbook.json. The code is on the page; open any scenario in Sandpack to edit it.
---

import LivePlayground from '../../../components/LivePlayground.astro';

Every scenario below is a live SDK demo running entirely in `local_only` mode — a bundled `revturbine.playbook.json` with plans, entitlements, placements, and targeting, and no decision-time network calls. The code is on the page; press **Open in Sandpack** on any scenario to edit it alongside a live preview, and switch demo users to see the engine respond.

## Fixed Slots

Always-visible inline placements.

<a id="fixed-button"></a>
### F-1: Upgrade Button
An always-visible upgrade CTA in a nav slot (`button`).
<LivePlayground scenarioId="fixed-button" />

<a id="fixed-in-page"></a>
### F-2: Plans & Pricing Card
An inline pricing/plans card (`in_page`).
<LivePlayground scenarioId="fixed-in-page" />

<a id="fixed-usage-counter"></a>
### F-3: Quota Meter
A usage meter with progress bar and counter (`in_page`).
<LivePlayground scenarioId="fixed-usage-counter" />

<a id="fixed-banner"></a>
### F-4: Annual Nudge Banner
An annual-plan nudge banner (`banner`).
<LivePlayground scenarioId="fixed-banner" />

## Access Gates

Entitlement gates that render an upsell when access is denied.

<a id="gate-modal"></a>
### G-1: Data Export Gate
A blocking modal gate on a paid feature (`modal`).
<LivePlayground scenarioId="gate-modal" />

<a id="gate-inline"></a>
### G-2: Branding Gate
An inline gate on a branding feature (`in_page`).
<LivePlayground scenarioId="gate-inline" />

<a id="gate-card"></a>
### G-3: Brand Kit Gate
A card gate on a brand-kit feature (`in_page`).
<LivePlayground scenarioId="gate-card" />

## Global Slots

Ephemeral, moment-driven messages.

<a id="msg-banner"></a>
### M-1: Usage Warning Banner
A "running low" usage banner (`banner`).
<LivePlayground scenarioId="msg-banner" />

<a id="msg-modal"></a>
### M-2: Usage Exhausted Modal
A limit-reached modal (`modal`).
<LivePlayground scenarioId="msg-modal" />

<a id="msg-toast"></a>
### M-3: Trial Welcome Toast
A trial-welcome toast (`toast`).
<LivePlayground scenarioId="msg-toast" />

## Headless API

The imperative SDK, no slot components.

<a id="headless-placement"></a>
### H-1: PlacementController
Driving a placement imperatively.
<LivePlayground scenarioId="headless-placement" />

<a id="headless-gate"></a>
### H-2: EntitlementGate
An entitlement check without React.
<LivePlayground scenarioId="headless-gate" />

<a id="headless-session"></a>
### H-3: SdkSession One-Shot
A one-shot session evaluation.
<LivePlayground scenarioId="headless-session" />

## How it works

Each scenario runs the SDK in an isolated Sandpack environment:

1. **`revturbine.playbook.json`** provides plans, entitlements, placements, surface templates, and targeting rules.
2. **`RevTurbineProvider`** initializes the SDK in `local_only` mode — no network calls.
3. **Demo users** (Alice, Bob, Carol, Dan, Eve, Frank) have different plans, usage, and entitlements.
4. **Targeting rules** evaluate against the user context to decide which placements to show.

Switch demo users in a scenario's code to see how the SDK renders different placements for different segments.
