---
title: "BannerSlot"
description: "Full-width banner placement — announcements, usage warnings, and promotional messages."
tableOfContents: false
sidebar:
  label: "BannerSlot"
---

import BannerSlotGallery from '../../../components/galleries/BannerSlotGallery';
import CodeExample from '../../../components/CodeExample';

export const code = `import { Slot } from "@revturbine/sdk";
import { DemoApp } from "./DemoApp";

// Bob is on Professional — the engine serves him an annual-upgrade banner.
export default function App() {
  return (
    <DemoApp user="user_bob">
      <Slot id="dashboard_top_banner" surfaceTemplateIds={["banner_placement"]} />
    </DemoApp>
  );
}
`;

A full-width banner for announcements, usage warnings, and time-sensitive promotions. Supports top/bottom positioning and optional dismiss.

**Content fields:** `header`, `body`, `cta_label`, `position` (`top` | `bottom`), `dismissible`

<BannerSlotGallery client:only="react" />

## Usage

A live, editable example — edit the code (or switch the demo user) and the preview re-runs:

<CodeExample client:only="react" code={code} />

The `BannerSlot` renders automatically when the decision engine selects a `banner_placement` template. Banners render at the top or bottom of the page. When `dismissible` is `true`, users can close the banner. The SDK tracks dismissals to prevent re-showing.

**Related:** [Playground → Annual Banner](/guides/placements/#fixed-banner) · [API Reference](/api/)
